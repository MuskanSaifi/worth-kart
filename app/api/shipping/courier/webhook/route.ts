/**
 * Shiprocket tracking webhook (alias URL — no "shiprocket" in path per panel rules).
 * Configure: https://worthkart.in/api/shipping/courier/webhook
 */
export { GET, POST } from "../../shiprocket/webhook/route";
